{
  "name": "filedroid-relay",
  "version": "1.0.0",
  "description": "Relay server for FileDroid tunnel — bridges TCP over WebSocket between devices",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
